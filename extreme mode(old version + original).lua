if not game:IsLoaded() then
	game.Loaded:Wait()
end


--: This is ass, Please do not ever leak this.
--: You can but credits me, Thank you!
--: Created by shuy1#3817 and anti-bypass by CommonSense12, LiterallyBacon!
local v = game.Players.LocalPlayer
if not game:IsLoaded() then
    game.Loaded:Wait()
end
if writefile then
   print("[Blacklist]: Anti-bypass is enabled, Checking if user is banned")
else
   print("[Blacklist]: Unable to activate anti-bypass due to player not having a workspace")
end

local Blacklisted = {
    ["tails_ciano"] = true, -- True or false (IF BANNED)
}

 --// playeradded is when the user is in game
game.Players.PlayerAdded:Connect(function(player)
    local username = game.Players.LocalPlayer.Name
    if Blacklisted[username] then
        v:Kick("You are blacklisted from my script lol")
    end
end)







if game:GetService("ReplicatedStorage"):FindFirstChild("balls") then



    firesignal(game:GetService("ReplicatedStorage").EntityInfo.Caption.OnClientEvent, 'why  would u exucute The mode 2 times bruh dumbasd, returning rn',true,1)



    return false



end



local Test = Instance.new("Part")



Test.Name = "balls"



Test.Parent = game:GetService("ReplicatedStorage")



Test = 1





Spawn1 = coroutine.wrap(function()

while task.wait(3.5) do

if workspace.Ambience_Seek.Playing == true then

			return

		end

		if workspace.Ambience_Figure.Playing == true then

			return

		end

		if workspace.Ambience_FigureEnd.Playing == true then

			return

		end

		if workspace.Ambience_FigureStart.Playing == true then

			return

		end

		loadstring(game:HttpGet("https://pastebin.com/raw/uXY1EAxZ"))()

end

end)

Spawn1()







function ReplaceAudGit(GithubSnd,SoundName) 

         local url=GithubSnd 

         if not isfile(SoundName..".mp3") then 

                 writefile(SoundName..".mp3", game:HttpGet(url)) 

         end 

         return (getcustomasset or getsynasset)(SoundName..".mp3") 

 end 

 workspace.Ambience_Seek.SoundId = ReplaceAudGit("https://github.com/Brololto/BUUMMM/blob/main/Screen_Recording_20230404-233303_YouTube%20(online-audio-converter.com).mp3?raw=true","RUN") 

 workspace.Ambience_Seek.Volume = 1







function ReplaceAudGit(GithubSnd,SoundName)

	local url=GithubSnd

	if not isfile(SoundName..".mp3") then

		writefile(SoundName..".mp3", game:HttpGet(url))

	end

	return (getcustomasset or getsynasset)(SoundName..".mp3")

end

local a = game.Players.LocalPlayer.PlayerGui.MainUI.Initiator.Main_Game.Health.Music

a.Blue.SoundId = ReplaceAudGit("https://github.com/Brololto/bad/blob/main/Screen_Recording_20230425-082026_Chrome%20(online-audio-converter.com).mp3?raw=true","ba1")

a.Blue.Volume = 7

function ReplaceAudGit(GithubSnd,SoundName)

	local url=GithubSnd

	if not isfile(SoundName..".mp3") then

		writefile(SoundName..".mp3", game:HttpGet(url))

	end

	return (getcustomasset or getsynasset)(SoundName..".mp3")

end





-- GuidingLight Music

coroutine.wrap(function()

    while true do

        wait(0.0005)

        game.ReplicatedStorage.GameData.LatestRoom.Changed:Wait()

        a.Blue.Volume = 7

    end

end)()




				
				



















































Spawn1 = coroutine.wrap(function()
while task.wait(0) do
local roomdoor = game.Workspace.CurrentRooms[game.ReplicatedStorage.GameData.LatestRoom.Value].Door.Door
		roomdoor.Material = "Ice"
		roomdoor.Sign.Material = "Ice"
		end
		end)
		Spawn1()	












game:GetService("ReplicatedStorage").GameData.LatestRoom:GetPropertyChangedSignal("Value"):Wait()  

    local function GetGitSound(GithubSnd,SoundName)
				local url=GithubSnd
				if not isfile(SoundName..".mp3") then
					writefile(SoundName..".mp3", game:HttpGet(url))
				end
				local sound=Instance.new("Sound")
				sound.SoundId=(getcustomasset or getsynasset)(SoundName..".mp3")
				return sound
			end
   local roar = GetGitSound("https://github.com/Tinkgy111/Bang/blob/main/Screen_Recording_20230519-203543_YouTube.mp3?raw=true","roarove")

				roar.Parent = workspace

				roar.Volume = 3

				roar:Play()
    firesignal(game:GetService("ReplicatedStorage").EntityInfo.Caption.OnClientEvent, 'Extreme mode Loaded',true,2)
wait(2)
    firesignal(game:GetService("ReplicatedStorage").EntityInfo.Caption.OnClientEvent, 'Credits cheese tinkgy#8163, ThatOneGuyRuben#5083 NormalBaran#3131 shuy1#3817 ThatOneAmyestCreature sandwich#9943',true,1)

wait(4)
local function GetGitSound(GithubSnd,SoundName)
				local url=GithubSnd
				if not isfile(SoundName..".mp3") then
					writefile(SoundName..".mp3", game:HttpGet(url))
				end
				local sound=Instance.new("Sound")
				sound.SoundId=(getcustomasset or getsynasset)(SoundName..".mp3")
				return sound
			end
   local Clock = GetGitSound("https://github.com/Brololto/bing-/blob/main/Screen_Recording_20230419-164136_Chrome%20(online-audio-converter.com).mp3?raw=true","Anotherlove")

				Clock.Parent = workspace

				Clock.Volume = 5

				Clock:Play()

				wait(1)

				game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = 9

				        local Bell = GetGitSound("https://github.com/Potato032/Entt/blob/main/ClockSoundsCreditsToVynixus.mp3?raw=true","Credit")

				Bell.Parent = workspace

				Bell.Volume = 5

				Bell:Play()

				game.Lighting.MainColorCorrection.TintColor = Color3.fromRGB(71, 151, 211)

	game.Lighting.MainColorCorrection.Contrast = 1

	local tween = game:GetService("TweenService")

	tween:Create(game.Lighting.MainColorCorrection, TweenInfo.new(2.5), {Contrast = 0}):Play()

	local TweenService = game:GetService("TweenService")

	local TW = TweenService:Create(game.Lighting.MainColorCorrection, TweenInfo.new(80),{TintColor = Color3.fromRGB(255, 255, 255)})

	TW:Play()
wait(34)
firesignal(game.ReplicatedStorage.EntityInfo.UseEventModule.OnClientEvent, "flicker", game.ReplicatedStorage.GameData.LatestRoom.Value, 1)
	wait(3)

			if not game.Players.LocalPlayer.Character:GetAttribute("Hiding") then

		game.Players.LocalPlayer.Character.Humanoid:TakeDamage(1000)

	game:GetService("ReplicatedStorage").GameStats["Player_".. game.Players.LocalPlayer.Name].Total.DeathCause.Value = "Silence"

	firesignal(game.ReplicatedStorage.EntityInfo.DeathHint.OnClientEvent, {"You died to Silence", "He is supposed to come after rush, to try to mimic him.", "But he gives you a little bit of time so use that for you Advantage.", "he will instanly kill you if you are not hiding"}, "Blue")

end

game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = 15


Spawn1 = coroutine.wrap(function()
while task.wait(math.random(5,120)) do
if game.ReplicatedStorage.GameData.LatestRoom.Value == 50 and workspace.Ambience_Seek.Playing == true then
game.ReplicatedStorage.GameData.LatestRoom.Value == 100
else
local static = Instance.new("Sound")
static.SoundId = "rbxassetid://9120425687"
static.Parent = game.ReplicatedStorage
static.Name = "Se"
static.Pitch = 0.6
static.Volume = 1.5
static.TimePosition = 0.2
static:Play()
local GUI = Instance.new("ScreenGui", game.Players.LocalPlayer.PlayerGui)
local Image = Instance.new("ImageLabel", GUI)

GUI.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

Image.BackgroundColor3 = Color3.fromRGB(255,255,255)
Image.BackgroundTransparency = 1
Image.Size = UDim2.new(1,0,1,0)
Image.Image = "rbxassetid://13362464118"
Image.ImageTransparency = 0
wait(1)
game.Players.LocalPlayer.PlayerGui.ScreenGui:Destroy()
	if game.Players.LocalPlayer.Character.Humanoid.MoveDirection ~= Vector3.new(0,0,0) then
		local Hum = game.Players.LocalPlayer.Character.Humanoid
Hum:TakeDamage(90)

			local blur = Instance.new("BlurEffect",game:GetService("Lighting"))
			blur.Size = 80
			game.SoundService.Main.EqualizerSoundEffect.Enabled = true
			game.SoundService.Main.EqualizerSoundEffect.HighGain = -50
			game.SoundService.Main.EqualizerSoundEffect.LowGain = 10
			game.SoundService.Main.EqualizerSoundEffect.MidGain = -50

			game:GetService("TweenService"):Create(game.SoundService.Main.EqualizerSoundEffect, TweenInfo.new(15, Enum.EasingStyle.Quint, Enum.EasingDirection.InOut), {
				LowGain = 0,
				HighGain = 0,
				MidGain = 0,
			}):Play()
			game:GetService("TweenService"):Create(blur,TweenInfo.new(15),{Size = 0}):Play()
			game:GetService("Debris"):AddItem(blur,15)
game:GetService("ReplicatedStorage").GameStats["Player_".. game.Players.LocalPlayer.Name].Total.DeathCause.Value = "Admiration"
	firesignal(game.ReplicatedStorage.EntityInfo.DeathHint.OnClientEvent, {"You have died to Admiration..", "Try your best to not move at all.", "He can sense you with sound."}, "Blue")
end


end

         end

end)
Spawn1() 



Spawn1 = coroutine.wrap(function()
while task.wait(300) do
if game.ReplicatedStorage.GameData.LatestRoom.Value == 50 and workspace.Ambience_Seek.Playing == true then
game.ReplicatedStorage.GameData.LatestRoom.Value == 100
else
game:GetService("ReplicatedStorage").GameData.LatestRoom:GetPropertyChangedSignal("Value"):Wait()  
local function GetGitSound(GithubSnd,SoundName)
				local url=GithubSnd
				if not isfile(SoundName..".mp3") then
					writefile(SoundName..".mp3", game:HttpGet(url))
				end
				local sound=Instance.new("Sound")
				sound.SoundId=(getcustomasset or getsynasset)(SoundName..".mp3")
				return sound
			end
   local Clock = GetGitSound("https://github.com/Brololto/bing-/blob/main/Screen_Recording_20230419-164136_Chrome%20(online-audio-converter.com).mp3?raw=true","Anotherlove")

				Clock.Parent = workspace

				Clock.Volume = 5

				Clock:Play()

				wait(1)

				game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = 9

				        local Bell = GetGitSound("https://github.com/Potato032/Entt/blob/main/ClockSoundsCreditsToVynixus.mp3?raw=true","Credit")

				Bell.Parent = workspace

				Bell.Volume = 5

				Bell:Play()

				game.Lighting.MainColorCorrection.TintColor = Color3.fromRGB(71, 151, 211)

	game.Lighting.MainColorCorrection.Contrast = 1

	local tween = game:GetService("TweenService")

	tween:Create(game.Lighting.MainColorCorrection, TweenInfo.new(2.5), {Contrast = 0}):Play()

	local TweenService = game:GetService("TweenService")

	local TW = TweenService:Create(game.Lighting.MainColorCorrection, TweenInfo.new(80),{TintColor = Color3.fromRGB(255, 255, 255)})

	TW:Play()
wait(34)
firesignal(game.ReplicatedStorage.EntityInfo.UseEventModule.OnClientEvent, "flicker", game.ReplicatedStorage.GameData.LatestRoom.Value, 1)
	wait(3)

			if not game.Players.LocalPlayer.Character:GetAttribute("Hiding") then

		game.Players.LocalPlayer.Character.Humanoid:TakeDamage(1000)

	game:GetService("ReplicatedStorage").GameStats["Player_".. game.Players.LocalPlayer.Name].Total.DeathCause.Value = "Silence"

	firesignal(game.ReplicatedStorage.EntityInfo.DeathHint.OnClientEvent, {"You died to Silence", "He is supposed to come after rush, to try to mimic him.", "But he gives you a little bit of time so use that for you Advantage.", "he will instanly kill you if you are not hiding"}, "Blue")

end

game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = 15
end
end
end)
Spawn1()











Spawn1 = coroutine.wrap(function()
while task.wait(645) do
if game.ReplicatedStorage.GameData.LatestRoom.Value == 50 and workspace.Ambience_Seek.Playing == true then
game.ReplicatedStorage.GameData.LatestRoom.Value == 100
else
		game:GetService("ReplicatedStorage").GameData.LatestRoom:GetPropertyChangedSignal("Value"):Wait()  
    local room = workspace.CurrentRooms[game.Players.LocalPlayer:GetAttribute("CurrentRoom")]

local color = Color3.fromRGB(239, 62, 255)

		room.LightBase.SurfaceLight.Enabled = true

		room.LightBase.SurfaceLight.Color = color

		for _, thing in pairs(room.Assets:GetDescendants()) do

			if thing:FindFirstChild"LightFixture" then

				thing.LightFixture.Neon.Color = color

				for _, light in pairs(thing.LightFixture:GetChildren()) do

					if light:IsA("SpotLight") or light:IsA("PointLight") then

						light.Color = color

					end

				end

			end

		end

		game.Lighting.MainColorCorrection.TintColor = Color3.fromRGB(62, 7, 12)

game.Lighting.MainColorCorrection.Contrast = 0.2

game.Lighting.MainColorCorrection.Saturation = -0.7

local tween = game:GetService("TweenService")

tween:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5), {Contrast = 0}):Play()

tween:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5), {Saturation = 0}):Play()

local TweenService = game:GetService("TweenService")

local TW = TweenService:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5),{TintColor = Color3.fromRGB(255, 255, 255)})

TW:Play()

		local function GetGitSound(GithubSnd,SoundName)

				local url=GithubSnd

				if not isfile(SoundName..".mp3") then

					writefile(SoundName..".mp3", game:HttpGet(url))

				end

				local sound=Instance.new("Sound")

				sound.SoundId=(getcustomasset or getsynasset)(SoundName..".mp3")

				return sound

			end

local Creator = loadstring(game:HttpGet("https://raw.githubusercontent.com/Check6969/Utilities/main/Doors%20Entity%20Spawner/Source.lua"))()

-- Create entity



local entity = Creator.createEntity({

    CustomName = "EnragedTerror", -- Custom name of your entity

    Model = "https://github.com/Brololto/NEWENRAGEDTERRORHARDCORE/blob/main/enrager_terroror.rbxm?raw=true", -- Can be GitHub file or rbxassetid

    Speed = 940, -- Percentage, 100 = default Rush speed

    DelayTime = 3, -- Time before starting cycles (seconds)

    HeightOffset = 10,

    CanKill = true,

    BreakLights = false,

    KillRange = 50,

    BackwardsMovement = false,

    FlickerLights = {

        false, -- Enabled

        1, -- Time (seconds)

    },

    Cycles = {

        Min = 1,

        Max = 4

    },

    CamShake = {

        true, -- Enabled

        {30, 30, 0.1, 1}, -- Shake values (don't change if you don't know)

        100, -- Shake start distance (from Entity to you)

    },

    Jumpscare = {

        false, -- Enabled ('false' if you don't want jumpscare)

        {

            Image1 = "rbxassetid://115830521", -- Image1 url

            Image2 = "rbxassetid://11708346230", -- Image2 url

            Shake = false,

            Sound1 = {

                0, -- SoundId

                { Volume = 0.5 }, -- Sound properties

            },

            Sound2 = {

                5567523008, -- SoundId

                { Volume = 0.5 }, -- Sound properties

            },

            Flashing = {

                true, -- Enabled

                Color3.fromRGB(6, 38, 135), -- Color

            },

            Tease = {

                false, -- Enabled ('false' if you don't want tease)

                Min = 1,

                Max = 3,

            },

        },

    },

    CustomDialog = {"its him"}, -- Custom death message (can be as long as you want)

})



-----[[ Advanced ]]-----

entity.Debug.OnEntitySpawned = function(entityModel)

    print("Entity has spawned:", entityModel)

end



entity.Debug.OnEntityDespawned = function(entityModel)

    print("Entity has despawned:", entityModel)
end



entity.Debug.OnEntityStartMoving = function(entityModel)

    print("Entity has started moving:", entityModel)

end



entity.Debug.OnEntityEnteredRoom = function(entityTable, room)

    print("Entity:", "has entered room:", room)

end



entity.Debug.OnEntityFinishedRebound = function(entityModel)

    print("Entity finished rebound:", entityModel)

end



entity.Debug.OnDeath = function()

    warn("You died.")

local crash = GetGitSound("https://github.com/Brololto/sound-crash-wii-not-anything-with-hardcore/blob/main/Screen_Recording_20230410-085142_YouTube%20(online-audio-converter.com).mp3?raw=true","crah")

			crash.Parent = workspace

			crash.Volume = 3

			crash:Play()

			while true do end

end

------------------------



-- Run the created entity

Creator.runEntity(entity)
end
end

end)
Spawn1()








Spawn1 = coroutine.wrap(function()
while task.wait(3.5) do
if game.ReplicatedStorage.GameData.LatestRoom.Value == 50 or game.ReplicatedStorage.GameData.LatestRoom.Value == 100 then

local Spawner = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors%20Entity%20Spawner/Source.lua"))()

-- Create entity

local entityTable = Spawner.createEntity({

    CustomName = "Hunger", -- Custom name of your entity

    Model = "rbxassetid://13989622160", -- Can be GitHub file or rbxassetid

    Speed = 38, -- Percentage, 100 = default Rush speed

    DelayTime = 0, -- Time before starting cycles (seconds)

    HeightOffset = 0,

    CanKill = true,

    KillRange = 6,

    BackwardsMovement = true,

    BreakLights = false,

    FlickerLights = {

        false, -- Enabled/Disabled

        1, -- Time (seconds)

    },

    Cycles = {

        Min = 1,

        Max = 500000000000000,

        WaitTime = 2,

    },

    CamShake = {

        false, -- Enabled/Disabled

        {2.5, 20, 0.1, 1}, -- Shake values (don't change if you don't know)

        100, -- Shake start distance (from Entity to you)

    },

    Jumpscare = {

        true, -- Enabled/Disabled

        {

            Image1 = "rbxassetid://5577595111", -- Image1 url

            Image2 = "rbxassetid://10657365561", -- Image2 url

            Shake = true,

            Sound1 = {

                4903742660, -- SoundId

                { Volume = 0.5 }, -- Sound properties

            },

            Sound2 = {

                3537873683, -- SoundId

                { Volume = 0.5 }, -- Sound properties

            },

            Flashing = {

                true, -- Enabled/Disabled

                Color3.fromRGB(0, 255, 255), -- Color

            },

            Tease = {

                true, -- Enabled/Disabled

                Min = 2,

                Max = 2,

            },

        },

    },

    CustomDialog = {"You Have Been Caught By Hunger...", "it lurks around on figures library.", "Don't Get Caught"}, -- Custom death message



})





-----[[  Debug -=- Advanced  ]]-----

entityTable.Debug.OnEntitySpawned = function()

    print("Entity has spawned:", entityTable)



end



entityTable.Debug.OnEntityDespawned = function()

    print("Entity has despawned:", entityTable)

end



entityTable.Debug.OnEntityStartMoving = function()

end



entityTable.Debug.OnEntityFinishedRebound = function()

    print("Entity has finished rebound:", entityTable)

end



entityTable.Debug.OnEntityEnteredRoom = function(room)

    print("Entity:", entityTable, "has entered room:", room)

end



entityTable.Debug.OnLookAtEntity = function()

    print("Player has looked at entity:", entityTable)

end



entityTable.Debug.OnDeath = function()

    warn("Player has died.")

    firesignal(game.ReplicatedStorage.EntityInfo.DeathHint.OnClientEvent, {"You Have Been Caught By Hunger...", "it lurks around on figures library.", "Don't Get Caught"}, "Blue")

end

------------------------------------





-- Run the created entity

Spawner.runEntity(entityTable)

end

end

end)
Spawn1()












Spawn1 = coroutine.wrap(function() 
while task.wait(189) do
if game.ReplicatedStorage.GameData.LatestRoom.Value == 50 and workspace.Ambience_Seek.Playing == true then
game.ReplicatedStorage.GameData.LatestRoom.Value == 100
else
game:GetService("ReplicatedStorage").GameData.LatestRoom:GetPropertyChangedSignal("Value"):Wait()   

			
	
				
	
	
	
	  local Smilersound = Instance.new("Sound", workspace)
			Smilersound.SoundId = "rbxassetid://9125713501"
			Smilersound:Play()
			Smilersound.Volume = 5        
	    
	-- Create entity
	    local room = workspace.CurrentRooms[game.Players.LocalPlayer:GetAttribute("CurrentRoom")]
	local color = Color3.fromRGB(255, 0, 0)
			room.LightBase.SurfaceLight.Enabled = true
			room.LightBase.SurfaceLight.Color = color
			for _, thing in pairs(room.Assets:GetDescendants()) do
				if thing:FindFirstChild"LightFixture" then
					thing.LightFixture.Neon.Color = color
					for _, light in pairs(thing.LightFixture:GetChildren()) do
						if light:IsA("SpotLight") or light:IsA("PointLight") then
							light.Color = color
						end
					end
				end
			end
	game.Lighting.MainColorCorrection.TintColor = Color3.fromRGB(255, 0, 0)
	game.Lighting.MainColorCorrection.Contrast = 1
	local tween = game:GetService("TweenService")
	tween:Create(game.Lighting.MainColorCorrection, TweenInfo.new(2.5), {Contrast = 0}):Play()
	local TweenService = game:GetService("TweenService")
	local TW = TweenService:Create(game.Lighting.MainColorCorrection, TweenInfo.new(80),{TintColor = Color3.fromRGB(255, 255, 255)})
	TW:Play()
local TW = game:GetService("TweenService")
local TWInfo = TweenInfo.new(3)

for i, v in pairs(workspace.CurrentRooms:GetDescendants()) do
    if v:IsA("PointLight") and v.Enabled == true then
        TW:Create(v, TWInfo, {Color = Color3.fromRGB(255, 0, 0)}):Play()
    end
    if v:IsA("SpotLight") and v.Enabled == true then
        TW:Create(v, TWInfo, {Color = Color3.fromRGB(255, 0, 0)}):Play()
    end
end
	local Creator = loadstring(game:HttpGet("https://raw.githubusercontent.com/Check6969/Utilities/main/Doors%20Entity%20Spawner/Source.lua"))()
	
	
	local entity = Creator.createEntity({
	    CustomName = "G-95", -- Custom name of your entity
	    Model = "https://github.com/Brololto/ExtremeModeG-95/blob/main/G-95Remastered-1.rbxm?raw=true", -- Can be GitHub file or rbxassetid
	    Speed = 170, -- Percentage, 100 = default Rush speed
	    DelayTime = 6, -- Time before starting cycles (seconds)
	    HeightOffset = 0,
	    CanKill = true,
	    BreakLights = false,
	    KillRange = 60,
	    BackwardsMovement = false,
	    FlickerLights = {
	        false, -- Enabled
	        2.5, -- Time (seconds)
	    },
	    Cycles = {
	        Min = 1,
	        Max = 1
	    },
	    CamShake = {
	        true, -- Enabled
	        {6, 15, 0.1, 1}, -- Shake values (don't change if you don't know)
	        100, -- Shake start distance (from Entity to you)
	    },
	    Jumpscare = {
	        true, -- Enabled ('false' if you don't want jumpscare)
	        {
	            Image1 = "rbxassetid://11394048190", -- Image1 url
	            Image2 = "rbxassetid://11395251069", -- Image2 url
	            Shake = false,
	            Sound1 = {
	                0, -- SoundId
	                { Volume = 0.5 }, -- Sound properties
	            },
	            Sound2 = {
	                5567523008, -- SoundId
	                { Volume = 0.5 }, -- Sound properties
	            },
	            Flashing = {
	                true, -- Enabled
	                Color3.fromRGB(6, 38, 135), -- Color
	            },
	            Tease = {
	                false, -- Enabled ('false' if you don't want tease)
	                Min = 1,
	                Max = 3,
	            },
	        },
	    },
	    CustomDialog = {"You died to G-95", "His scream can let you Know hes coming, Hide quickly when he does this.", "But he gives you a little bit of time so use that for you Advantage."}, -- Custom death message (can be as long as you want)
	})
	
	-----[[ Advanced ]]-----
	entity.Debug.OnEntitySpawned = function(entityModel)
	    print("Entity has spawned:", entityModel)
	

local Idle = GetGitSound("https://github.com/Brololto/G95-MOVING/blob/main/Screen_Recording_20230323-172501_YouTube%20(online-audio-converter.com)%20(1).mp3?raw=true","ang")
				Idle.Parent = workspace
				Idle.Volume = 20
	    Idle:Play()
	end
	
	entity.Debug.OnEntityDespawned = function(entityModel)
	    print("Entity has despawned:", entityModel)
	    local Slam = GetGitSound("https://github.com/Brololto/FUCKYOUFACCIST/blob/main/Screen_Recording_20230407-114843_YouTube%20(online-audio-converter.com).mp3?raw=true","Slam")
				Slam.Parent = workspace
				Slam.Volume = 20
				Slam:Play() 
	end
	
	entity.Debug.OnEntityStartMoving = function(entityModel)
	    print("Entity has started moving:", entityModel)
	end
	
	entity.Debug.OnEntityEnteredRoom = function(entityTable, room)
	    print("Entity:", "has entered room:", room)
	end
	
	entity.Debug.OnEntityFinishedRebound = function(entityModel)
	    print("Entity finished rebound:", entityModel)
	    local Achievements = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Custom%20Achievements/Source.lua"))()
	
	-- Creates and displays your custom achievement
	Achievements.Get({
	    Title = "Screames Of the Dammed.",
	    Desc = "don't be to slow.",
	    Reason = "Encounter G-95",
	    Image = "https://github.com/Brololto/SCP-BADGE/blob/main/glimpse1.png?raw=true",
	   })
	end
	
	entity.Debug.OnDeath = function()
	    warn("You died.")
	    firesignal(game.ReplicatedStorage.EntityInfo.DeathHint.OnClientEvent, {"You died to G-95", "His scream can let you Know hes coming, Hide quickly when he does this.", "But he gives you a little bit of time so use that for you Advantage."}, "Blue")
	end
	
	------------------------
	
	-- Run the created entity
	Creator.runEntity(entity)
	end
    end
end)
Spawn1()
















Spawn1 = coroutine.wrap(function()
while task.wait(320) do
if game.ReplicatedStorage.GameData.LatestRoom.Value == 50 and workspace.Ambience_Seek.Playing == true then
game.ReplicatedStorage.GameData.LatestRoom.Value == 100
else
game:GetService("ReplicatedStorage").GameData.LatestRoom:GetPropertyChangedSignal("Value"):Wait()   

function GetGitSound(GithubSnd,SoundName)
	local url=GithubSnd
	if not isfile(SoundName..".mp3") then
		writefile(SoundName..".mp3", game:HttpGet(url))
	end
	local sound=Instance.new("Sound")
	sound.SoundId=(getcustomasset or getsynasset)(SoundName..".mp3")
	return sound
end
local Spawner = loadstring(game:HttpGet("https://raw.githubusercontent.com/Check6969/Utilities/main/Doors%20Entity%20Spawner/Source.lua"))()


-- Create entity
local entityTable = Spawner.createEntity({
    CustomName = "Trauma", -- Custom name of your entity
    Model = "https://github.com/Brololto/NEWV2/blob/main/BRAIN_DAMAGED_TRAUMA.rbxm?raw=true", -- Can be GitHub file or rbxassetid
    Speed = 600, -- Percentage, 100 = default Rush speed
    DelayTime = 9, -- Time before starting cycles (seconds)
    HeightOffset = 0,
    CanKill = true,
    KillRange = 50,
    BackwardsMovement = false,
    BreakLights = true,
    FlickerLights = {
        false, -- Enabled/Disabled
        1, -- Time (seconds)
    },
    Cycles = {
        Min = 1,
        Max = 1,
        WaitTime = 2,
    },
    CamShake = {
        false, -- Enabled/Disabled
        {500, 500, 500, 1}, -- Shake values (don't change if you don't know)
        100, -- Shake start distance (from Entity to you)
    },
    Jumpscare = {
        false, -- Enabled/Disabled
        {
            Image1 = "rbxassetid://10483855823", -- Image1 url
            Image2 = "rbxassetid://10483999903", -- Image2 url
            Shake = true,
            Sound1 = {
                10483790459, -- SoundId
                { Volume = 0.5 }, -- Sound properties
            },
            Sound2 = {
                10483837590, -- SoundId
                { Volume = 0.5 }, -- Sound properties
            },
            Flashing = {
                true, -- Enabled/Disabled
                Color3.fromRGB(255, 255, 255), -- Color
            },
            Tease = {
                true, -- Enabled/Disabled
                Min = 1,
                Max = 3,
            },
        },
    },
    CustomDialog = {"You Died To Trauma", " This entity is fast and quick", " Hide As Soon As Possible if heard."}, -- Custom death message
})


-----[[  Debug -=- Advanced  ]]-----
entityTable.Debug.OnEntitySpawned = function()
    print("Entity has spawned:", entityTable)
            local sound = GetGitSound("https://github.com/Brololto/TraumaDistoredRepentV3/blob/main/Screen_Recording_20230411-184126_YouTube%20(online-audio-converter.com).mp3?raw=true","yeah")
    sound.Parent = workspace
    sound.Volume = 3
	sound:Play()
end

entityTable.Debug.OnEntityDespawned = function()
    print("Entity has despawned:", entityTable)
end

entityTable.Debug.OnEntityStartMoving = function()
    print("Entity has started moving:", entityTable)

end

entityTable.Debug.OnEntityFinishedRebound = function()
    print("Entity has finished rebound:", entityTable)
end

entityTable.Debug.OnEntityEnteredRoom = function(room)
    print("Entity:", entityTable, "has entered room:", room)
       
end

entityTable.Debug.OnLookAtEntity = function()
    print("Player has looked at entity:", entityTable)
end

entityTable.Debug.OnDeath = function()
    warn("Player has died.")
    sound:Stop()

end
------------------------------------


-- Run the created entity
Spawner.runEntity(entityTable)
game.ReplicatedStorage.GameData.LatestRoom.Changed:Wait()
function GetGitSound(GithubSnd,SoundName)
	local url=GithubSnd
	if not isfile(SoundName..".mp3") then
		writefile(SoundName..".mp3", game:HttpGet(url))
	end
	local sound=Instance.new("Sound")
	sound.SoundId=(getcustomasset or getsynasset)(SoundName..".mp3")
	return sound
end
local Spawner = loadstring(game:HttpGet("https://raw.githubusercontent.com/Check6969/Utilities/main/Doors%20Entity%20Spawner/Source.lua"))()


-- Create entity
local entityTable = Spawner.createEntity({
    CustomName = "Trauma", -- Custom name of your entity
    Model = "https://github.com/Brololto/NEWV2/blob/main/BRAIN_DAMAGED_TRAUMA.rbxm?raw=true", -- Can be GitHub file or rbxassetid
    Speed = 600, -- Percentage, 100 = default Rush speed
    DelayTime = 5, -- Time before starting cycles (seconds)
    HeightOffset = 0,
    CanKill = true,
    KillRange = 100,
    BackwardsMovement = true,
    BreakLights = true,
    FlickerLights = {
        false, -- Enabled/Disabled
        1, -- Time (seconds)
    },
    Cycles = {
        Min = 1,
        Max = 1,
        WaitTime = 2,
    },
    CamShake = {
        false, -- Enabled/Disabled
        {500, 500, 500, 1}, -- Shake values (don't change if you don't know)
        100, -- Shake start distance (from Entity to you)
    },
    Jumpscare = {
        false, -- Enabled/Disabled
        {
            Image1 = "rbxassetid://10483855823", -- Image1 url
            Image2 = "rbxassetid://10483999903", -- Image2 url
            Shake = true,
            Sound1 = {
                10483790459, -- SoundId
                { Volume = 0.5 }, -- Sound properties
            },
            Sound2 = {
                10483837590, -- SoundId
                { Volume = 0.5 }, -- Sound properties
            },
            Flashing = {
                true, -- Enabled/Disabled
                Color3.fromRGB(255, 255, 255), -- Color
            },
            Tease = {
                true, -- Enabled/Disabled
                Min = 1,
                Max = 3,
            },
        },
    },
    CustomDialog = {"You Died To Trauma", " This entity is fast and quick", " Hide As Soon As Possible if heard."}, -- Custom death message
})


-----[[  Debug -=- Advanced  ]]-----
entityTable.Debug.OnEntitySpawned = function()
    print("Entity has spawned:", entityTable)

end

entityTable.Debug.OnEntityDespawned = function()
    print("Entity has despawned:", entityTable)
    
local Achievements = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Custom%20Achievements/Source.lua"))()

-- Creates and displays your custom achievement
Achievements.Get({
    Title = "AnPMqq.",
    Desc = "iuuIOlaP.",
    Reason = "Encounter Trauma",
    Image = "https://github.com/Brololto/Trauma-Badge/blob/main/pngaaa.com-690531.png?raw=true",
    })
end

entityTable.Debug.OnEntityStartMoving = function()
    print("Entity has started moving:", entityTable)

end

entityTable.Debug.OnEntityFinishedRebound = function()
    print("Entity has finished rebound:", entityTable)
end

entityTable.Debug.OnEntityEnteredRoom = function(room)
    print("Entity:", entityTable, "has entered room:", room)

end

entityTable.Debug.OnLookAtEntity = function()
    print("Player has looked at entity:", entityTable)
end

entityTable.Debug.OnDeath = function()
    warn("Player has died.")
    sound:Stop()

end
------------------------------------


-- Run the created entity
Spawner.runEntity(entityTable)
end
end)
Spawn1()








Spawn1 = coroutine.wrap(function()
while task.wait(0) do
if workspace.Ambience_Seek.Playing or workspace.Ambience_Figure.Playing or workspace.Ambience_FigureIntense.Playing then
if workspace:FindFirstChild("Trauma") then
workspace.Trauma:Destroy()
end
end
end
end)
Spawn1()






Spawn1 = coroutine.wrap(function()
while task.wait(420) do
if workspace.Ambience_Seek.Playing == true then

			return

		end

		if workspace.Ambience_Figure.Playing == true then

			return

		end

		if workspace.Ambience_FigureEnd.Playing == true then

			return

		end

		if workspace.Ambience_FigureStart.Playing == true then

			return

		end
game:GetService("ReplicatedStorage").GameData.LatestRoom:GetPropertyChangedSignal("Value"):Wait()  
local function GetGitSound(GithubSnd,SoundName)
				local url=GithubSnd
				if not isfile(SoundName..".mp3") then
					writefile(SoundName..".mp3", game:HttpGet(url))
				end
				local sound=Instance.new("Sound")
				sound.SoundId=(getcustomasset or getsynasset)(SoundName..".mp3")
				return sound
			end--Hmm needed
local IsSpawned = false

--Chang LIGGHHTTSSS!!!!!
function ChangeLights(LightColor, Delay)
local TW = game:GetService("TweenService")
local TWInfo = TweenInfo.new(Delay)

for i, v in pairs(workspace.CurrentRooms:GetDescendants()) do
    if v:IsA("PointLight") and v.Enabled == true then
        TW:Create(v, TWInfo, {Color = LightColor}):Play()
    end
    if v:IsA("SpotLight") and v.Enabled == true then
        TW:Create(v, TWInfo, {Color = LightColor}):Play()
    end
end
end

local Spawner = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors%20Entity%20Spawner/Source.lua"))()


-- Create entity
local entityTable = Spawner.createEntity({
    CustomName = "V88", -- Custom name of your entity
    Model = "https://github.com/Tonkgy836/Iwannasuckadickbitchyougotwih/blob/main/V-88.rbxm?raw=true", -- Can be GitHub file or rbxassetid
    Speed = 260, -- Percentage, 100 = default Rush speed
    DelayTime = 5, -- Time before starting cycles (seconds)
    HeightOffset = 0,
    CanKill = true,
    KillRange = 50,
    BackwardsMovement = false,
    BreakLights = false,
    FlickerLights = {
        false, -- Enabled/Disabled
        1, -- Time (seconds)
    },
    Cycles = {
        Min = 2,
        Max = 4,
        WaitTime = 1,
    },
    CamShake = {
        true, -- Enabled/Disabled
        {8, 9, 0.1, 1}, -- Shake values (don't change if you don't know)
        100, -- Shake start distance (from Entity to you)
    },
    Jumpscare = {
        false, -- Enabled/Disabled
        {
            Image1 = "rbxassetid://10483855823", -- Image1 url
            Image2 = "rbxassetid://10483999903", -- Image2 url
            Shake = true,
            Sound1 = {
                10483790459, -- SoundId
                { Volume = 0.5 }, -- Sound properties
            },
            Sound2 = {
                10483837590, -- SoundId
                { Volume = 0.5 }, -- Sound properties
            },
            Flashing = {
                true, -- Enabled/Disabled
                Color3.fromRGB(255, 255, 255), -- Color
            },
            Tease = {
                true, -- Enabled/Disabled
                Min = 1,
                Max = 3,
            },
        },
    },
    CustomDialog = {"You can", "put your", "custom death", "message here."}, -- Custom death message
})


-----[[  Debug -=- Advanced  ]]-----
entityTable.Debug.OnEntitySpawned = function()
    --WE GOTTA CHANGE THE LIGHTS WITH THIS ONE🗣️🔥🔥
    ChangeLights(Color3.fromRGB(0, 0, 255), 3)


    --Needed
    IsBlueGuySpawned = true
    
    --Stuff
    local Model = workspace:WaitForChild("V88")
    local Parte = Model:WaitForChild("RushNew")
    
    --Face N' Stuff
    local ImageGroup = Parte:WaitForChild("faces")
    local Images = ImageGroup:GetChildren()
    
    --The More Needed
    local Attach = Parte:WaitForChild("Main")
    local TheFACE = Attach:WaitForChild("face")
    
    --I FORGOR.
    TheFACE.LockedToPart = true
    
    task.spawn(function()
        while IsBlueGuySpawned == true do
            local RandomBullshit = Imagesmath.random(1, #Images)
            wait(0.25)
            TheFACE.Texture = RandomBullshit.Image
        end
    end)
end

--100% needed so that it doesn't die
entityTable.Debug.OnEntityDespawned = function()
    IsBlueGuySpawned = false
end

--SCARU JUMPSCAR!!1!1111!!!
entityTable.Debug.OnDeath = function()
    warn("Player has died.")
        local Se = GetGitSound("https://github.com/Tonkgy836/V-88DEATHER/blob/main/Screen_Recording_20230429-121753_YouTube%20(online-audio-converter.com).mp3?raw=true","Deh")
			Se.Parent = workspace
			Se.Volume = 9
			Se:Play()
end
------------------------------------


-- Run the created entity
Spawner.runEntity(entityTable)
end
end)
Spawn1()



Spawn1 = coroutine.wrap(function()
while task.wait(510) do
if game.ReplicatedStorage.GameData.LatestRoom.Value == 50 and workspace.Ambience_Seek.Playing == true then
game.ReplicatedStorage.GameData.LatestRoom.Value == 100
else
game:GetService("ReplicatedStorage").GameData.LatestRoom:GetPropertyChangedSignal("Value"):Wait()  
	local function GetGitSound(GithubSnd,SoundName)

				local url=GithubSnd

				if not isfile(SoundName..".mp3") then

					writefile(SoundName..".mp3", game:HttpGet(url))

				end

				local sound=Instance.new("Sound")

				sound.SoundId=(getcustomasset or getsynasset)(SoundName..".mp3")

				return sound

			end


	 

	    

	-- Create entity


	local Creator = loadstring(game:HttpGet("https://raw.githubusercontent.com/Check6969/Utilities/main/Doors%20Entity%20Spawner/Source.lua"))()

	

	

	local entity = Creator.createEntity({

	    CustomName = "Specimen 2", -- Custom name of your entity

	    Model = "https://github.com/Brololto/NEWV2/blob/main/specimen_2_remodel_fixed.rbxm?raw=true", -- Can be GitHub file or rbxassetid

	    Speed = 35, -- Percentage, 100 = default Rush speed

	    DelayTime = 0, -- Time before starting cycles (seconds)

	    HeightOffset = 0,

	    CanKill = true,

	    BreakLights = false,

	    KillRange = 50,

	    BackwardsMovement = false,

	    FlickerLights = {

	        true, -- Enabled

	        80, -- Time (seconds)

	    },

	    Cycles = {

	        Min = 1,

	        Max = 1

	    },

	    CamShake = {

	        true, -- Enabled

	        {6, 15, 0.1, 1}, -- Shake values (don't change if you don't know)

	        100, -- Shake start distance (from Entity to you)

	    },

	    Jumpscare = {

	        false, -- Enabled ('false' if you don't want jumpscare)

	        {

	            Image1 = "rbxassetid://11394048190", -- Image1 url

	            Image2 = "rbxassetid://11395251069", -- Image2 url

	            Shake = false,

	            Sound1 = {

	                0, -- SoundId

	                { Volume = 0.5 }, -- Sound properties

	            },

	            Sound2 = {

	                5567523008, -- SoundId

	                { Volume = 0.5 }, -- Sound properties

	            },

	            Flashing = {

	                true, -- Enabled

	                Color3.fromRGB(6, 38, 135), -- Color

	            },

	            Tease = {

	                false, -- Enabled ('false' if you don't want tease)

	                Min = 1,

	                Max = 3,

	            },

	        },

	    },

	    CustomDialog = {"You died to Specimen 2", "You Have To Run From This Entity To survive, so run when he spawns."}, -- Custom death message (can be as long as you want)

	})

	

	-----[[ Advanced ]]-----

	entity.Debug.OnEntitySpawned = function(entityModel)

	    print("Entity has spawned:", entityModel)
	
	

	end

	

	entity.Debug.OnEntityDespawned = function(entityModel)

	    print("Entity has despawned:", entityModel)

	

	end

	

	entity.Debug.OnEntityStartMoving = function(entityModel)

	    print("Entity has started moving:", entityModel)
local Move = GetGitSound("https://github.com/Brololto/Specimen-2-running-music/blob/main/Screen_Recording_20230322-160706_YouTube%20(audio-extractor.net).mp3?raw=true","saitama")

			Move.Parent = workspace

			Move.Volume = 3

			Move:Play()
	end

	

	entity.Debug.OnEntityEnteredRoom = function(entityTable, room)

	    print("Entity:", "has entered room:", room)

	end

	

	entity.Debug.OnEntityFinishedRebound = function(entityModel)

	    print("Entity finished rebound:", entityModel)

	

	end

	

	entity.Debug.OnDeath = function()

	    warn("You died.")

	    firesignal(game.ReplicatedStorage.EntityInfo.DeathHint.OnClientEvent, {"You died to Specimen 2", "You Have To Run From This Entity To survive, so run when he spawns."}, "Blue")

	end

	

	------------------------

	

	-- Run the created entity

	Creator.runEntity(entity)
	end
end
end)
Spawn1()














Spawn1 = coroutine.wrap(function()
while task.wait(399) do
if game.ReplicatedStorage.GameData.LatestRoom.Value == 50 and workspace.Ambience_Seek.Playing == true then
game.ReplicatedStorage.GameData.LatestRoom.Value == 100
else
game:GetService("ReplicatedStorage").GameData.LatestRoom:GetPropertyChangedSignal("Value"):Wait()  
    local room = workspace.CurrentRooms[game.Players.LocalPlayer:GetAttribute("CurrentRoom")]
	local color = Color3.fromRGB(50, 205, 50)
			room.LightBase.SurfaceLight.Enabled = true
			room.LightBase.SurfaceLight.Color = color
			for _, thing in pairs(room.Assets:GetDescendants()) do
				if thing:FindFirstChild"LightFixture" then
					thing.LightFixture.Neon.Color = color
					for _, light in pairs(thing.LightFixture:GetChildren()) do
						if light:IsA("SpotLight") or light:IsA("PointLight") then
							light.Color = color
						end
					end
				end
			end
	game.Lighting.MainColorCorrection.TintColor = Color3.fromRGB(50, 205, 50)
	game.Lighting.MainColorCorrection.Contrast = 1
	local tween = game:GetService("TweenService")
	tween:Create(game.Lighting.MainColorCorrection, TweenInfo.new(2.5), {Contrast = 0}):Play()
	local TweenService = game:GetService("TweenService")
	local TW = TweenService:Create(game.Lighting.MainColorCorrection, TweenInfo.new(80),{TintColor = Color3.fromRGB(255, 255, 255)})
	TW:Play()
local TW = game:GetService("TweenService")
local TWInfo = TweenInfo.new(3)

for i, v in pairs(workspace.CurrentRooms:GetDescendants()) do
    if v:IsA("PointLight") and v.Enabled == true then
        TW:Create(v, TWInfo, {Color = Color3.fromRGB(128, 128, 128)}):Play()
    end
    if v:IsA("SpotLight") and v.Enabled == true then
        TW:Create(v, TWInfo, {Color = Color3.fromRGB(128, 128, 128)}):Play()
    end
end
local Spawner = loadstring(game:HttpGet("https://raw.githubusercontent.com/Check6969/Utilities/main/Doors%20Entity%20Spawner/Source.lua"))()


-- Create entity
local entityTable = Spawner.createEntity({
    CustomName = "Cycle", -- Custom name of your entity
    Model = "https://github.com/Check6969/Utilities/blob/main/Models/Cycle.rbxm?raw=true", -- Can be GitHub file or rbxassetid
    Speed = 400, -- Percentage, 100 = default Rush speed
    DelayTime = 5, -- Time before starting cycles (seconds)
    HeightOffset = 0,
    CanKill = true,
    KillRange = 40,
    BackwardsMovement = false,
    BreakLights = true,
    FlickerLights = {
        false, -- Enabled/Disabled
        1, -- Time (seconds)
    },
    Cycles = {
        Min = 1,
        Max = 3,
        WaitTime = 1,
    },
    CamShake = {
        false, -- Enabled/Disabled
        {500, 500, 500, 1}, -- Shake values (don't change if you don't know)
        100, -- Shake start distance (from Entity to you)
    },
    Jumpscare = {
        false, -- Enabled/Disabled
        {
            Image1 = "rbxassetid://10483855823", -- Image1 url
            Image2 = "rbxassetid://10483999903", -- Image2 url
            Shake = true,
            Sound1 = {
                10483790459, -- SoundId
                { Volume = 0.5 }, -- Sound properties
            },
            Sound2 = {
                10483837590, -- SoundId
                { Volume = 0.5 }, -- Sound properties
            },
            Flashing = {
                true, -- Enabled/Disabled
                Color3.fromRGB(255, 255, 255), -- Color
            },
            Tease = {
                true, -- Enabled/Disabled
                Min = 1,
                Max = 3,
            },
        },
    },
    CustomDialog = {"You Died to Cycle", "He rebounds 3 times", "Hide Fast."}, -- Custom death message
})


-----[[  Debug -=- Advanced  ]]-----
entityTable.Debug.OnEntitySpawned = function()
    print("Entity has spawned:", entityTable)

end

entityTable.Debug.OnEntityDespawned = function()
    print("Entity has despawned:", entityTable)

end

entityTable.Debug.OnEntityStartMoving = function()
    print("Entity has started moving:", entityTable)

end

entityTable.Debug.OnEntityFinishedRebound = function()
    print("Entity has finished rebound:", entityTable)
end

entityTable.Debug.OnEntityEnteredRoom = function(room)
    print("Entity:", entityTable, "has entered room:", room)

end

entityTable.Debug.OnLookAtEntity = function()
    print("Player has looked at entity:", entityTable)
end

entityTable.Debug.OnDeath = function()
    warn("Player has died.")
    

end
------------------------------------


-- Run the created entity
Spawner.runEntity(entityTable)
end
end
end)
Spawn1()











function a60()

function GetGitSound(GithubSnd,SoundName)
	local url=GithubSnd
	if not isfile(SoundName..".mp3") then
		writefile(SoundName..".mp3", game:HttpGet(url))
	end
	local sound=Instance.new("Sound")
	sound.SoundId=(getcustomasset or getsynasset)(SoundName..".mp3")
	return sound
end

local Spawner = loadstring(game:HttpGet("https://raw.githubusercontent.com/Check6969/Utilities/main/Doors%20Entity%20Spawner/Source.lua"))()


-- Create entity
local entityTable = Spawner.createEntity({
    CustomName = "A-60", -- Custom name of your entity
    Model = "https://github.com/Tinkgy111/9/blob/main/A-60Epic.rbxm?raw=true", -- Can be GitHub file or rbxassetid
    Speed = 87, -- Percentage, 100 = default Rush speed
    DelayTime = 3, -- Time before starting cycles (seconds)
    HeightOffset = 0,
    CanKill = false,
    KillRange = 100,
    BackwardsMovement = false,
    BreakLights = true,
    FlickerLights = {
        false, -- Enabled/Disabled
        1, -- Time (seconds)
    },
    Cycles = {
        Min = 1,
        Max = 1,
        WaitTime = 1,
    },
    CamShake = {
        true, -- Enabled/Disabled
        {40, 40, 0.1, 1}, -- Shake values (don't change if you don't know)
        100, -- Shake start distance (from Entity to you)
    },
    Jumpscare = {
        false, -- Enabled/Disabled
        {
            Image1 = "rbxassetid://10483855823", -- Image1 url
            Image2 = "rbxassetid://10483999903", -- Image2 url
            Shake = true,
            Sound1 = {
                10483790459, -- SoundId
                { Volume = 0.5 }, -- Sound properties
            },
            Sound2 = {
                10483837590, -- SoundId
                { Volume = 0.5 }, -- Sound properties
            },
            Flashing = {
                true, -- Enabled/Disabled
                Color3.fromRGB(255, 255, 255), -- Color
            },
            Tease = {
                true, -- Enabled/Disabled
                Min = 1,
                Max = 3,
            },
        },
    },
    CustomDialog = {"You Died to Rebound", "He rebounds each door you open", "so pay attention to his initial spawn."}, -- Custom death message
})


-----[[  Debug -=- Advanced  ]]-----
entityTable.Debug.OnEntitySpawned = function()
    print("Entity has spawned:", entityTable)

end

entityTable.Debug.OnEntityDespawned = function()
    print("Entity has despawned:", entityTable)
    local CameraShaker = require(game.ReplicatedStorage.CameraShaker)
local camara = game.Workspace.CurrentCamera
local camShake = CameraShaker.new(Enum.RenderPriority.Camera.Value, function(shakeCf)
	camara.CFrame = camara.CFrame * shakeCf
end)
camShake:Start()
camShake:ShakeOnce(105,100,0,5.5,1,0)
game.Lighting.MainColorCorrection.TintColor = Color3.fromRGB(255, 0, 0)
game.Lighting.MainColorCorrection.Contrast = 0.2
game.Lighting.MainColorCorrection.Saturation = -0.7
local tween = game:GetService("TweenService")
tween:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5), {Contrast = 0}):Play()
tween:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5), {Saturation = 0}):Play()
local TweenService = game:GetService("TweenService")
local TW = TweenService:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5),{TintColor = Color3.fromRGB(255, 255, 255)})
TW:Play()
end

entityTable.Debug.OnEntityStartMoving = function()
    print("Entity has started moving:", entityTable)

end

entityTable.Debug.OnEntityFinishedRebound = function()
    print("Entity has finished rebound:", entityTable)
end

entityTable.Debug.OnEntityEnteredRoom = function(room)
    print("Entity:", entityTable, "has entered room:", room)
    if not game.Players.LocalPlayer.Character:GetAttribute("Hiding") then

		game.Players.LocalPlayer.Character.Humanoid:TakeDamage(1000)

	game:GetService("ReplicatedStorage").GameStats["Player_".. game.Players.LocalPlayer.Name].Total.DeathCause.Value = "A-60"
	
            local Jumpscare = GetGitSound("https://github.com/Tinkgy111/Scarce/blob/main/Screen_Recording_20230520-151330_YouTube.mp3?raw=true","niii")
    Jumpscare.Parent = workspace
    Jumpscare.Volume = 4
	Jumpscare:Play()
    -- UI Construction
local ambasing = {"rbxassetid://192267375", "rbxassetid://1972219024", "rbxassetid://1822114127", "rbxassetid://3413871766", "rbxassetid://3354536350", "rbxassetid://4749623118", "rbxassetid://1857893092", "rbxassetid://4508624823", "rbxassetid://6309704436", "rbxassetid://10458678325", "rbxassetid://11857886160"}

    local JumpscareGui = Instance.new("ScreenGui")
    local Background = Instance.new("Frame")
    local Face = Instance.new("ImageLabel")

    JumpscareGui.Name = "JumpscareGui"
    JumpscareGui.IgnoreGuiInset = true
    JumpscareGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
    JumpscareGui.Parent = game.Players.LocalPlayer:WaitForChild("PlayerGui")

    Background.Name = "Background"
    Background.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
    Background.BorderSizePixel = 0
    Background.Size = UDim2.new(1, 0, 1, 0)
    Background.ZIndex = 999
    Background.BackgroundTransparency = 0.6

    Face.Name = "Face"
    Face.AnchorPoint = Vector2.new(0.5, 0.5)
    Face.BackgroundTransparency = 1
    Face.Position = UDim2.new(0.5, 0, 0.5, 0)
    Face.ResampleMode = Enum.ResamplerMode.Pixelated
    Face.Size = UDim2.new(0, 0, 0, 0)
    Face.Image = ambasing[math.random(1, #ambasing)]
    Face.ImageColor3 = Color3.fromRGB(0, 0, 0)
    Face.ImageTransparency = 0

    Face.Parent = Background
    Background.Parent = JumpscareGui

    game.TweenService:Create(Face, TweenInfo.new(0.2), {Size = UDim2.new(0, 850, 0, 850), ImageTransparency = 0}):Play()
    game.TweenService:Create(Face, TweenInfo.new(0.2), {Rotation = math.random(-10,10)}):Play()
    task.wait(0.2)
    game.TweenService:Create(Face, TweenInfo.new(0.6), {ImageTransparency = 1}):Play()
game.TweenService:Create(Background, TweenInfo.new(2.6), {BackgroundTransparency = 1}):Play()
    task.wait(2.65)
    game.TweenService:Create(Static, TweenInfo.new(1.6), {ImageTransparency = 0}):Play()
game.TweenService:Create(Background, TweenInfo.new(1.6), {BackgroundTransparency = 0}):Play()
task.wait(3)
game.TweenService:Create(Static, TweenInfo.new(1), {ImageColor3 = Color3.fromRGB(255, 255, 255)}):Play()
task.wait(1)
game.TweenService:Create(Static, TweenInfo.new(0.1), {Size =  UDim2.new(2, 0, 0.05, 0)}):Play()
task.wait(0.05)
game.TweenService:Create(Static, TweenInfo.new(0.2), {Size =  UDim2.new(0, 0, 0, 0)}):Play()
task.wait(2)
game.TweenService:Create(Background, TweenInfo.new(1.6), {BackgroundTransparency = 1}):Play()
task.wait(1.6)
JumpscareGui:Destroy()
wait(6)
end
 wait(1)
wait(6)
end

entityTable.Debug.OnLookAtEntity = function()
    print("Player has looked at entity:", entityTable)
end

entityTable.Debug.OnDeath = function()
    warn("Player has died.")
    game:GetService("ReplicatedStorage").GameStats["Player_".. game.Players.LocalPlayer.Name].Total.DeathCause.Value = "A-60"
            local Jumpscare = GetGitSound("https://github.com/Tinkgy111/Scarce/blob/main/Screen_Recording_20230520-151330_YouTube.mp3?raw=true","niii")
    Jumpscare.Parent = workspace
    Jumpscare.Volume = 4
	Jumpscare:Play()
    -- UI Construction
local ambasing = {"rbxassetid://192267375", "rbxassetid://1972219024", "rbxassetid://1822114127", "rbxassetid://3413871766", "rbxassetid://3354536350", "rbxassetid://4749623118", "rbxassetid://1857893092", "rbxassetid://4508624823", "rbxassetid://6309704436", "rbxassetid://10458678325", "rbxassetid://11857886160"}

    local JumpscareGui = Instance.new("ScreenGui")
    local Background = Instance.new("Frame")
    local Face = Instance.new("ImageLabel")

    JumpscareGui.Name = "JumpscareGui"
    JumpscareGui.IgnoreGuiInset = true
    JumpscareGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
    JumpscareGui.Parent = game.Players.LocalPlayer:WaitForChild("PlayerGui")

    Background.Name = "Background"
    Background.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
    Background.BorderSizePixel = 0
    Background.Size = UDim2.new(1, 0, 1, 0)
    Background.ZIndex = 999
    Background.BackgroundTransparency = 0.6

    Face.Name = "Face"
    Face.AnchorPoint = Vector2.new(0.5, 0.5)
    Face.BackgroundTransparency = 1
    Face.Position = UDim2.new(0.5, 0, 0.5, 0)
    Face.ResampleMode = Enum.ResamplerMode.Pixelated
    Face.Size = UDim2.new(0, 0, 0, 0)
    Face.Image = ambasing[math.random(1, #ambasing)]
    Face.ImageColor3 = Color3.fromRGB(0, 0, 0)
    Face.ImageTransparency = 0

    Face.Parent = Background
    Background.Parent = JumpscareGui

    game.TweenService:Create(Face, TweenInfo.new(0.2), {Size = UDim2.new(0, 850, 0, 850), ImageTransparency = 0}):Play()
    game.TweenService:Create(Face, TweenInfo.new(0.2), {Rotation = math.random(-10,10)}):Play()
    task.wait(0.2)
    game.TweenService:Create(Face, TweenInfo.new(0.6), {ImageTransparency = 1}):Play()
game.TweenService:Create(Background, TweenInfo.new(2.6), {BackgroundTransparency = 1}):Play()
    task.wait(2.65)
    game.TweenService:Create(Static, TweenInfo.new(1.6), {ImageTransparency = 0}):Play()
game.TweenService:Create(Background, TweenInfo.new(1.6), {BackgroundTransparency = 0}):Play()
task.wait(3)
game.TweenService:Create(Static, TweenInfo.new(1), {ImageColor3 = Color3.fromRGB(255, 255, 255)}):Play()
task.wait(1)
game.TweenService:Create(Static, TweenInfo.new(0.1), {Size =  UDim2.new(2, 0, 0.05, 0)}):Play()
task.wait(0.05)
game.TweenService:Create(Static, TweenInfo.new(0.2), {Size =  UDim2.new(0, 0, 0, 0)}):Play()
task.wait(2)
game.TweenService:Create(Background, TweenInfo.new(1.6), {BackgroundTransparency = 1}):Play()
task.wait(1.6)
JumpscareGui:Destroy()
end
------------------------------------


-- Run the created entity
Spawner.runEntity(entityTable)
end
end







Spawn1 = coroutine.wrap(function()
while task.wait(3.5) do
if game.ReplicatedStorage.GameData.LatestRoom.Value == 50 and workspace.Ambience_Seek.Playing == true then
game.ReplicatedStorage.GameData.LatestRoom.Value == 100
else
if game.ReplicatedStorage.GameData.LatestRoom.Value == 62 or game.ReplicatedStorage.GameData.LatestRoom.Value == 94 then
a60()
end
end
end
end)
Spawn1()



function whisper()

 
firesignal(game:GetService("ReplicatedStorage").EntityInfo.Caption.OnClientEvent, 'Do Not Move',true,6)   

function GetGitSound(GithubSnd,SoundName)
	local url=GithubSnd
	if not isfile(SoundName..".mp3") then
		writefile(SoundName..".mp3", game:HttpGet(url))
	end
	local sound=Instance.new("Sound")
	sound.SoundId=(getcustomasset or getsynasset)(SoundName..".mp3")
	return sound
end
    local room = workspace.CurrentRooms[game.Players.LocalPlayer:GetAttribute("CurrentRoom")]
	local color = Color3.fromRGB(128, 128, 128)
			room.LightBase.SurfaceLight.Enabled = true
			room.LightBase.SurfaceLight.Color = color
			for _, thing in pairs(room.Assets:GetDescendants()) do
				if thing:FindFirstChild"LightFixture" then
					thing.LightFixture.Neon.Color = color
					for _, light in pairs(thing.LightFixture:GetChildren()) do
						if light:IsA("SpotLight") or light:IsA("PointLight") then
							light.Color = color
						end
					end
				end
			end
	game.Lighting.MainColorCorrection.TintColor = Color3.fromRGB(128, 128, 128)
	game.Lighting.MainColorCorrection.Contrast = 1
	local tween = game:GetService("TweenService")
	tween:Create(game.Lighting.MainColorCorrection, TweenInfo.new(2.5), {Contrast = 0}):Play()
	local TweenService = game:GetService("TweenService")
	local TW = TweenService:Create(game.Lighting.MainColorCorrection, TweenInfo.new(80),{TintColor = Color3.fromRGB(255, 255, 255)})
	TW:Play()
local TW = game:GetService("TweenService")
local TWInfo = TweenInfo.new(3)

for i, v in pairs(workspace.CurrentRooms:GetDescendants()) do
    if v:IsA("PointLight") and v.Enabled == true then
        TW:Create(v, TWInfo, {Color = Color3.fromRGB(128, 128, 128)}):Play()
    end
    if v:IsA("SpotLight") and v.Enabled == true then
        TW:Create(v, TWInfo, {Color = Color3.fromRGB(128, 128, 128)}):Play()
    end
end
local Spawner = loadstring(game:HttpGet("https://raw.githubusercontent.com/Potato032/Random/main/TheFixedSpawner"))()


-- Create entity
local entityTable = Spawner.createEntity({
    CustomName = "Whisper", -- Custom name of your entity
    Model = "https://github.com/Check6969/Utilities/blob/main/Models/wgipxdssdd.rbxm?raw=true", -- Can be GitHub file or rbxassetid
    Speed = 50, -- Percentage, 100 = default Rush speed
    DelayTime = 1.4, -- Time before starting cycles (seconds)
    HeightOffset = 1,
    CanKill = false,
    KillRange = 50,
    BackwardsMovement = false,
    BreakLights = false,
    FlickerLights = {
        false, -- Enabled/Disabled
        1, -- Time (seconds)
    },
    Cycles = {
        Min = 1,
        Max = 1,
        WaitTime = 1,
    },
    CamShake = {
        true, -- Enabled/Disabled
        {3.5, 20, 0.1, 1}, -- Shake values (don't change if you don't know)
        100, -- Shake start distance (from Entity to you)
    },
    Jumpscare = {
        false, -- Enabled/Disabled
        {
            Image1 = "rbxassetid://10483855823", -- Image1 url
            Image2 = "rbxassetid://10483999903", -- Image2 url
            Shake = true,
            Sound1 = {
                10483790459, -- SoundId
                { Volume = 0.5 }, -- Sound properties
            },
            Sound2 = {
                10483837590, -- SoundId
                { Volume = 0.5 }, -- Sound properties
            },
            Flashing = {
                true, -- Enabled/Disabled
                Color3.fromRGB(255, 255, 255), -- Color
            },
            Tease = {
                true, -- Enabled/Disabled
                Min = 1,
                Max = 3,
            },
        },
    },
    CustomDialog = {"You Died To A Unknown Entity Called Whisperer..", "His presence is known when you start to hear whispers.", "The Whispers Are Everywhere And you cant escape it..","you have to stop moving to avoid him.."}, -- Custom death message
    firesignal(game.ReplicatedStorage.EntityInfo.DeathHint.OnClientEvent, {"You Died To A Unknown Entity Called Whisperer..", "His presence is known when you start to hear whispers.", "The Whispers Are Everywhere And you cant escape it.", "You need to stop moving to avoid it", "as soon you hear it stop moving immediately."}, "Blue")
})


-----[[  Debug -=- Advanced  ]]-----
entityTable.Debug.OnEntitySpawned = function()
    print("Entity has spawned:", entityTable)
            local whisper = GetGitSound("https://github.com/Check6969/Sounds/blob/main/Whisper.mp3?raw=true","Whispering_Sound")
            whisper.Parent = workspace
            whisper.Volume = 3
            whisper:Play()
                        for i, v in next, {workspace.Whisper, workspace.Whisper:GetDescendants()} do
     if v:IsA("Part") or v:IsA("MeshPart") then
        v.CanCollide = false
     end
end
   Whisper.CanCollide = false
end

entityTable.Debug.OnEntityDespawned = function()
    print("Entity has despawned:", entityTable)
    local Achievements = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Custom%20Achievements/Source.lua"))()

-- Creates and displays your custom achievement
Achievements.Get({
    Title = "Whispering Everywhere..",
    Desc = "Do you hear that..?",
    Reason = "Survived Whisper",
    Image = GitPNG("https://github.com/Check6969/floor-2/blob/main/135_sin_titulo3.png?raw=true","wowe"),
})
workspace.CurrentRooms[game.ReplicatedStorage.GameData.LatestRoom.Value]:WaitForChild("Door").ClientOpen:FireServer()
end

entityTable.Debug.OnEntityStartMoving = function()
    print("Entity has started moving:", entityTable)

end

entityTable.Debug.OnEntityFinishedRebound = function()
    print("Entity has finished rebound:", entityTable)
end

entityTable.Debug.OnEntityEnteredRoom = function(room)
    print("Entity:", entityTable, "has entered room:", room)
    wait(0.3)
        if game.Players.LocalPlayer.Character.Humanoid.MoveDirection ~= Vector3.new(0,0,0) then
        game.Players.LocalPlayer.Character.Humanoid:TakeDamage(1000)
        whisper:Stop()
game:GetService("ReplicatedStorage").GameStats["Player_".. game.Players.LocalPlayer.Name].Total.DeathCause.Value = "Whisperer"
	firesignal(game.ReplicatedStorage.EntityInfo.DeathHint.OnClientEvent, {"You Died To A Unknown Entity Called Whisperer..", "His presence is known when you start to hear whispers.", "The Whispers Are Everywhere And you cant escape it.", "You need to stop moving to avoid it", "as soon you hear it stop moving immediately."}, "Blue")
                 -- Instances:

local ScreenGui = Instance.new("ScreenGui")
local Frame = Instance.new("Frame")
local ImageLabel = Instance.new("ImageLabel")

--Properties:

ScreenGui.Parent = game.Players.LocalPlayer:WaitForChild("PlayerGui")
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

Frame.Parent = ScreenGui
Frame.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
Frame.BackgroundTransparency = 0.500
Frame.BorderSizePixel = 0
Frame.Size = UDim2.new(1, 0, 1, 0)
Frame.Visible = true

ImageLabel.Parent = Frame
ImageLabel.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
ImageLabel.BackgroundTransparency = 1.000
ImageLabel.Position = UDim2.new(0.349999994, 0, 0.300000012, 0)
ImageLabel.Size = UDim2.new(0.349999994, 0, 0.400000006, 0)
ImageLabel.Visible = true
ImageLabel.Image = "rbxassetid://13507277926"

local JumpscareSound = Instance.new("Sound", workspace)
JumpscareSound.SoundId = "rbxassetid://5567523008"
JumpscareSound.PlaybackSpeed = 0.5
JumpscareSound.TimePosition = 0.25
JumpscareSound.Volume = 1

local Distorsion = Instance.new("DistortionSoundEffect", JumpscareSound)
Distorsion.Level = 0.9

local PitchShift = Instance.new("PitchShiftSoundEffect", JumpscareSound)
PitchShift.Octave = 1.25

JumpscareSound:Play()
task.spawn(function()
	while ScreenGui do task.wait(0.04)
		Frame.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
		ImageLabel.Rotation = Random.new():NextNumber(-50, 50)
		ImageLabel.Position = UDim2.new(Random.new():NextNumber(0, 0.65), 0, Random.new():NextNumber(0, 0.6), 0) task.wait(0.04)
		Frame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
		ImageLabel.Rotation = Random.new():NextNumber(-50, 50)
		ImageLabel.Position = UDim2.new(Random.new():NextNumber(0, 0.65), 0, Random.new():NextNumber(0, 0.6), 0)
	end
end)

task.wait(0.7)

ScreenGui:Destroy()
JumpscareSound:Destroy()
game.Players.LocalPlayer.PlayerGui.ScreenGui:Destroy()
	end
end

entityTable.Debug.OnLookAtEntity = function()
    print("Player has looked at entity:", entityTable)
end

entityTable.Debug.OnDeath = function()
    warn("Player has died.")


end
------------------------------------


-- Run the created entity
Spawner.runEntity(entityTable)
end
end


Spawn1 = coroutine.wrap(function()
while task.wait(220) do
if game.ReplicatedStorage.GameData.LatestRoom.Value == 50 and workspace.Ambience_Seek.Playing == true then
game.ReplicatedStorage.GameData.LatestRoom.Value == 100
else
game.ReplicatedStorage.GameData.LatestRoom.Changed:Wait()
whisper()
end
end
end)
Spawn1()



Spawn1 = coroutine.wrap(function()
while task.wait(0) do
if game.ReplicatedStorage.GameData.LatestRoom.Value == 60 then
function darkness()
local woomy = 0.1
game.Lighting.FogEnd = "20"
    game.Lighting.FogStart = "5"
    game.Lighting.FogColor = Color3.fromRGB(0, 0, 0)
	task.wait(woomy)
	for i, v in pairs(workspace.CurrentRooms:GetDescendants()) do
		if v.Parent == workspace.CurrentRooms then
			v.LightBase.SurfaceLight.Enabled = false
		end
		pcall(function()
		if v:IsA("PointLight") or v:IsA("SpotLight") then
			v.Enabled = false
		end
		if v.Name == "LightStand" then
			local aaaa = v:FindFirstChild("LightFixture")
			if aaaa then
				aaaa.PointLight.Enabled = false
				aaaa.Neon:Destroy()
			end
		end
		if v.Name == "Chandelier" then
			local aaaa = v:FindFirstChild("LightFixture")
			if aaaa then
				aaaa.PointLight.Enabled = false
				aaaa.SpotLight.Enabled = false
				aaaa.Neon:Destroy()
			end
		end
		if v.Name == "Fireplace" then
			local aaaa = v:FindFirstChild("Fireplace_Logs")
			if aaaa then
				aaaa:FindFirstChild("FireLight", true).Enabled = false
				aaaa:FindFirstChild("FireParticles", true).Enabled = false
				aaaa:FindFirstChild("SmokeParticles", true).Enabled = false
				aaaa:FindFirstChild("SparkParticles", true).Enabled = false
				if not aaaa:FindFirstChild("PlaySound2", true) then
				aaaa:FindFirstChild("PlaySound", true).Name = "PlaySound2"
				end
				aaaa:FindFirstChild("PlaySound", true).Playing = false
				aaaa:FindFirstChild("PlaySound2", true).Playing = false
			end
		end
		end)
	end
end
darkness()
game:GetService("ReplicatedStorage").GameData.LatestRoom.Changed:Connect(darkness)
local function GetGitSound(GithubSnd,SoundName)
				local url=GithubSnd
				if not isfile(SoundName..".mp3") then
					writefile(SoundName..".mp3", game:HttpGet(url))
				end
				local sound=Instance.new("Sound")
				sound.SoundId=(getcustomasset or getsynasset)(SoundName..".mp3")
				return sound
			end
       local Darkness = GetGitSound("https://github.com/Tinkgy111/The-Gloomy-Darkness-Audio-/blob/main/Screen_Recording_20230524-164509_YouTube.mp3?raw=true","Gloomy")

				Darkness.Parent = workspace

				Darkness.Volume = 5

				Darkness:Play()
local shut = game.Players.LocalPlayer.PlayerGui.MainUI.MainFrame.Intro
local intro = shut:Clone()
intro.Parent = game.Players.LocalPlayer.PlayerGui.MainUI
intro.Name = "Custom"
intro.Visible = true
intro.Text = "The Gloomy Darkness"
local underline = UDim2.new(0.45, 0, 0.015, 6)
game.TweenService:Create(intro.Underline, TweenInfo.new(1), {Size = underline}):Play()
wait(5)
game.TweenService:Create(intro.Underline, TweenInfo.new(1.3), {Size = UDim2.new(0.4, 0, 0.015, 6)}):Play()
wait(1)
game.TweenService:Create(intro.Underline, TweenInfo.new(2), {ImageTransparency = 1}):Play()
game.TweenService:Create(intro, TweenInfo.new(2), {TextTransparency = 1}):Play()
game.TweenService:Create(intro.Underline, TweenInfo.new(7), {Size = UDim2.new(0, 0, 0.015, 6)}):Play()
intro.Visible = true
intro.TextTransparency = 0
intro.Underline.ImageTransparency = 0
wait(8)
intro:Destroy()
end
end
end)
Spawn1()